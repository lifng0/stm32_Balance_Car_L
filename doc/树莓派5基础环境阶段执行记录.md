# 树莓派5基础环境阶段执行记录

更新时间：2026-06-02

## 1. 本次实际执行结论

树莓派基础环境阶段主体已完成，当前已经具备进入下一阶段 `STM32 <-> 树莓派` 串口联调的条件。

已完成：

- 确认 `SSH` 免密登录可用
- 确认 `sudo` 免密可用
- 确认系统时区为 `Asia/Shanghai`
- 完成系统更新检查
- 安装常用基础工具
- 安装串口排查工具
- 安装 `Docker`
- 安装 `Docker Compose`
- 启用 `docker` 服务开机自启
- 将用户 `lifng0` 加入 `docker` 用户组
- 创建项目工作目录

## 2. 当前系统实际状态

本次远程检查得到的当前树莓派实际系统信息如下：

- 用户：`lifng0`
- 主机名：`lifng0`
- 架构：`aarch64`
- 内核：`6.12.75+rpt-rpi-2712`
- 系统：`Debian GNU/Linux 13 (trixie)`
- 时区：`Asia/Shanghai`

说明：

- 用户口头描述是“临时换成 `Raspberry Pi OS 64-bit`”
- 但当前机器实际回报的 `/etc/os-release` 为 `Debian 13 (trixie)`
- 后续所有配置和兼容性判断，应以机器当前实际系统为准

## 3. 已安装的关键组件

### 基础工具

- `git`
- `curl`
- `wget`
- `vim`
- `tmux`
- `htop`
- `net-tools`
- `build-essential`
- `python3-pip`
- `python3-venv`

### 串口排查工具

- `minicom`
- `screen`
- `usbutils`

### 容器相关

- `docker.io`
- `docker-compose`

## 4. Docker 当前状态

当前验证结果：

- `docker --version` 正常
- `docker compose version` 正常
- `docker` 服务状态为 `active`
- `docker` 服务状态为 `enabled`
- 普通用户 `lifng0` 已进入 `docker` 用户组

## 5. 已创建目录

当前已创建：

- `~/workspace/balance_car/docker`
- `~/workspace/balance_car/ws/src`
- `~/workspace/balance_car/scripts`
- `~/workspace/balance_car/logs`
- `~/workspace/balance_car/services`

## 6. 当前遗留问题

### Docker Hub 访问超时

在本次阶段验收中发现：

- 树莓派可正常访问 `apt` 软件源
- 但访问 `https://registry-1.docker.io/v2/` 超时
- 因此 `docker run hello-world` 暂未跑通

这说明：

- `Docker` 本体和服务已经装好
- 但树莓派到 `Docker Hub` 的网络连通性仍需单独处理
- 后续如果要拉取 `ROS 2` 镜像，这个问题需要先解决

## 7. 对下一阶段的影响

对下一阶段 `STM32 <-> 树莓派` 串口联调来说：

- 不构成立即阻塞
- 因为串口协议设计、串口桥原型、设备识别都可以先做

对后续 `ROS 2` 容器化来说：

- 构成潜在阻塞
- 因为后续拉基础镜像时仍会受 `Docker Hub` 访问问题影响

## 8. 建议的下一步

建议按下面顺序继续：

1. 先进入 `STM32 <-> 树莓派` 串口协议设计
2. 完成 `TTL转USB` 方式的最小串口收发联调
3. 在并行空档单独排查 `Docker Hub` 访问超时问题

